https://www.codewars.com/kata/5a262cfb8f27f217f700000b/train/javascript

function solve(a,b){
    let s1 = Array.from(a).filter(el => !b.includes(el)).join('');
    let s2 = Array.from(b).filter(el => !a.includes(el)).join('');
    return s1 + s2;
}
