https://www.codewars.com/kata/55467aaf72494e3bdc00007f/train/javascript

function isRotation(s1, s2){
if (s1.length != s2.length)
            return false;
        let temp = s1 + s1
        if (temp.indexOf(s2) != -1) {
            return true;
        } else {
            return false;
        }
    }
