https://www.codewars.com/kata/52aaf51822e82a808100066b/train/javascript


function replaceAll(input, find, replace) {
  if(input == "") {
    return replace;
  }
  if(find == ""){
    return replace+input.split(find).join(replace)+replace;
  }
  return input.split(find).join(replace);
}
