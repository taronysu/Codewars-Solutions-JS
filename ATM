https://www.codewars.com/kata/5635e7cb49adc7b54500001c/train/javascript

function solve(n) {
  if (n % 10 != 0) return -1;
  let count = 0;
  arr = [500,200,100,50,20,10];
  for (el of arr) {
    while (n >= el) {
      n -= el;
      count += 1;
    }
  }
  return count;
  }
