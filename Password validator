https://www.codewars.com/kata/56a921fa8c5167d8e7000053/train/javascript

function password(str) {
  let l = str.length >= 8;
  let num = str.match(/[0-9]/);
  let lower = str.match(/[a-z]/);
  let upper = str.match(/[A-Z]/);
   return l && num && lower && upper ? true : false;
}
