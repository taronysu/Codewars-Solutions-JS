https://www.codewars.com/kata/578aa45ee9fd15ff4600090d/train/javascript

function sortArray(arr) {
 const odd = arr.filter(el => el % 2 !== 0).sort((a, b) => b - a);
    return arr.map(x => (x % 2 === 0 ? x : odd.pop()));
}
