https://www.codewars.com/kata/59a1cdde9f922b83ee00003b/train/javascript

function stantonMeasure(arr){
  let arr1 = [];
  let arr2 = [];
  
  arr.map(a => (a == 1) ? arr1.push(a): null);  
  arr.map(a => (a== arr1.length) ? arr2.push(a): null);
  
  return arr2.length;
}
