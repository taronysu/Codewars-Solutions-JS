https://www.codewars.com/kata/58daa7617332e59593000006/train/javascript

function findLongest(arr){
const m = Math.max(...arr.map(el => String(el).length));
    for (const el of arr) {
        if (String(el).length === m) {
            return el;
        }
    }
}
