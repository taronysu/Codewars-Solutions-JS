https://www.codewars.com/kata/5544c7a5cb454edb3c000047/train/javascript

function bouncingBall(h,  bounce,  window) {
  let rb = -1;
  if (bounce > 0 && bounce < 1)
    while (h > window) rb += 2, h *= bounce;
  return rb;
}
